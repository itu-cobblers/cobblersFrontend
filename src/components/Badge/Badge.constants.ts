import type { BadgeTone } from './Badge.types'

export const BADGE_TONE_CLASS: Record<BadgeTone, string> = {
  lang: 'rounded border border-accent/40 bg-accent/10 px-2 py-[3px] text-[11px] font-semibold uppercase tracking-[0.6px] text-accent',
}
