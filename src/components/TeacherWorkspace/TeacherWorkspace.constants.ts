/** Same spaced-card rhythm as STUDENT_WORKSPACE_MAIN_CLASS. */
export const TEACHER_BODY_CLASS = 'relative z-10 flex flex-1 gap-3 overflow-hidden p-3'

/** The assignment column between the roster and the code viewer. */
export const TEACHER_PANEL_COLUMN_CLASS = 'flex min-w-0 flex-[4] flex-col overflow-hidden'
