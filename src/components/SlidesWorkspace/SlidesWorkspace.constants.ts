// No rail — the deck fills the full width of the tab. Same outer gutter as
// `STUDENT_WORKSPACE_MAIN_CLASS` so the two top-level tabs feel consistent.
export const SLIDES_WORKSPACE_MAIN_CLASS = 'relative z-10 flex flex-1 flex-col overflow-hidden gap-3 p-3'
