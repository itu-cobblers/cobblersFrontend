// Same row shape as `STUDENT_WORKSPACE_MAIN_CLASS` — rail + content, one gap, one padding.
export const SLIDES_WORKSPACE_MAIN_CLASS = 'relative z-10 flex flex-1 gap-3 overflow-hidden p-3'

// Stacks an optional `TeacherFollowBanner` above the viewer, same as `STUDENT_WORKSPACE_CONTENT_COLUMN_CLASS`.
export const SLIDES_WORKSPACE_CONTENT_COLUMN_CLASS = 'flex min-w-0 flex-1 flex-col overflow-hidden gap-3'

export const SLIDES_WORKSPACE_LOADING_CLASS = 'flex flex-1 items-center justify-center text-sm text-muted-foreground'
