/**
 * `justify-end`, not `justify-between`: the controls are the only child now
 * that the view-status label is gone, and `between` would park them left.
 */
export const ASSIGNMENT_FOOTER_CLASS =
  'flex shrink-0 items-center justify-end gap-2.5'