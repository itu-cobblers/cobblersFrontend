/**
 * The room code at the size a class can read off a projector — the job the big
 * ROOM CODE block in the rail used to do, now on demand instead of costing
 * permanent rail height.
 */
export const ROOM_CODE_MODAL_CLASS = 'flex flex-col items-center gap-4 py-2 text-center'

export const ROOM_CODE_MODAL_LABEL_CLASS =
  'text-[11px] font-bold uppercase tracking-[0.3em] text-muted-foreground'

/** `tabular-nums` so the glyphs stay evenly spaced whatever code is generated. */
export const ROOM_CODE_MODAL_VALUE_CLASS =
  'font-mono text-7xl font-extrabold tabular-nums tracking-[0.15em] text-foreground'

export const ROOM_CODE_MODAL_HINT_CLASS = 'text-sm text-muted-foreground'

export const ROOM_CODE_MODAL_LABEL = 'Room code'

export const ROOM_CODE_MODAL_HINT = 'Students enter this on the BootCode start screen.'
